
<!-- 
 After deployment:
  1. New Lambda Function (glaciar-stack-harmonix-platform-scheduler) will be created
  2. EventBridge Rules will trigger it daily:
    - 3:15 AM GMT-3: Stops Harmonix (ECS → RDS)
    - 9:00 AM GMT-3: Starts Harmonix (RDS → ECS)
  3. Existing Lambda will continue handling EC2 instances only
   -->

  Manual Testing

  To manually start Harmonix platform NOW:

  aws lambda invoke \
    --function-name glaciar-stack-harmonix-platform-scheduler \
    --payload '{"action": "start"}' \
    response.json && cat response.json

  This will:
  1. Start RDS database
  2. Scale ECS Backstage service to 2 tasks
  3. Harmonix will be available in ~3-5 minutes


  